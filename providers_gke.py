"""GKE provider stubs.

This module is a placeholder for future GKE-specific tabs and actions.
Keep functions small and focused; return the same menu dicts used by app.py.
"""

from typing import List, Dict, Any


def get_tabs(config: Dict[str, Any]) -> List[Dict[str, Any]]:
    """Return additional tabs for GKE (future).

    Currently returns an empty list to avoid behavior changes.
    """
    return []

